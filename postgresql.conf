# postgresql.conf

listen_addresses = '*'
# 💡 Augmente le cache des données. Ceci est CRITIQUE.
shared_buffers = 512MB 

# 💡 Mémoire par opération (tri, historique des mouvements)
work_mem = 32MB
